[project]
name = "agent"
version = "0.1.0"
description = "Thermal receipt print agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-escpos>=3.0",
    "pywin32>=306",
    "websockets>=12.0",
]
